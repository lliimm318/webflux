spring:
  r2dbc:
    url: r2dbc:mysql://localhost:3306/flux
    username: root
    password: qwe123


spring.main.allow-bean-definition-overriding: true

auth:
  jwt:
    exp:
      access: ${ACCESS_EXP:3600}
      refresh: ${REFRESH_EXP:10000}